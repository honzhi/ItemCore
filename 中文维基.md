# ItemCore Wiki

> 版本 1.0.8 | Paper 1.21.x | Java 21 | 可选依赖：PlaceholderAPI、BetterHud

---

## 📑 目录

- [1. 简介](#1-简介)
- [2. 安装与配置](#2-安装与配置)
- [3. 命令系统](#3-命令系统)
- [4. 权限系统](#4-权限系统)
- [5. 物品创建](#5-物品创建)
- [6. 属性系统](#6-属性系统)
- [7. 元素系统](#7-元素系统)
- [8. 伤害系统](#8-伤害系统)
- [9. 套装系统](#9-套装系统)
- [10. Lore 展示系统](#10-lore-展示系统)
- [11. 物品库 GUI](#11-物品库-gui)
- [拓展1 技能系统(ItemCoreMythic)](#拓展1-技能系统itemcoremythic)
- [附录1 PlaceholderAPI 占位符](#附录1-placeholderapi-占位符)
- [附录2 开发者 API](#附录2-开发者-api)
- [常见问题](#常见问题)

---

## 1. 简介

ItemCore 是一个功能集中的物品库插件，核心只负责物品定义、属性计算、伤害计算、元素系统、套装、Lore 展示与物品库 GUI，其余玩法通过拓展插件接入。

**已有的拓展插件：**

| 插件 | 用途 |
|------|------|
| **ItemCoreMythic** | MythicMobs 桥接：物品技能触发、`icdamage` / `icheal` 技能机制 |
| **ItemCoreRPG** | 伤害飘字、玩家属性面板（`/icrpg info`） |
| **ItemCoreForge** | 锻造 / 合成拓展 |
| **ItemCoreTrinkets** | 饰品栏拓展 |
| **ItemCoreLevel** | 玩家等级拓展 |
| **ItemCoreEnhance** | 强化 / 镶嵌拓展 |
| **ItemCoreTitles** | 称号拓展 |
| **ItemCoreQuest** | 任务拓展 |
| **ItemCoreAchievements** | 成就拓展 |
| **ItemCoreUltimateShop** | UltimateShop 商店对接 |

**核心特性：**

- 25 种自定义属性，支持固定值、随机范围与随机词条
- 品质（固定 / 随机权重）、套装、自定义耐久、物品绑定
- 独特的元素系统（元素积累 / 异常状态）
- 法力值系统（可选开启，支持 BetterHud 提示）
- 实时刷新玩家物品 Lore 与属性，`/ic reload` 热重载
- 完整的附属开发 API（属性提供器、Lore 提供器、分类行为系统等）

---

## 2. 安装与配置

### 2.1 安装步骤

1. 将 `ItemCore.jar` 放入 `plugins/` 目录（需要 Paper 1.21.x 与 Java 21）
2. 可选安装 PlaceholderAPI（占位符）、BetterHud（法力不足 HUD 提示）
3. 重启服务器，首次启动会自动生成配置文件：

```
plugins/ItemCore/
├── config.yml              # 主配置
├── attributes.yml          # 属性全局参数（暴击、适应之力、减伤公式、穿透顺序）
├── messages.yml            # 消息配置
├── categories.yml          # 分类配置
├── ranks.yml               # 品质配置
├── sets.yml                # 套装配置
├── elements.yml            # 元素配置
├── ailments.yml            # 异常配置
├── items/                  # 物品配置目录（文件名由 categories.yml 的 items_file 决定）
│   ├── melee_weapons.yml
│   ├── ranged_weapons.yml
│   ├── armor.yml
│   ├── material.yml
│   ├── consumable.yml
│   ├── blocks.yml
│   └── tools.yml
└── tooltip/
    ├── lore.yml            # Lore 布局
    └── stats.yml           # 属性显示格式
```

### 2.2 config.yml 主配置

```yaml
language: "zh-CN"          # zh-CN / en-US
debug_mode: false          # 调试日志

combat:
  # 禁用全服原版护甲/护甲韧性减伤；开启后 ARMOR 属性也失效，请改用 PHYSICAL_RESIST
  # 保护类附魔不受影响
  disable_vanilla_armor: false

# 物品全局保护（物品配置中写同名键可覆盖）
Item_global_settings:
  disable_anvil: true            # 禁止铁砧操作（修复/合并/改名）
  disable_enchant: true          # 禁止附魔（附魔台与铁砧附魔书合并）
  disable_crafting_table: true   # 禁止作为原版合成材料

# 法力值系统
mana:
  enabled: false
  base: 100                  # 基础最大法力
  base_regeneration: 1       # 每秒基础恢复
  not_enough_display: chat   # chat / hud / both
  hud:                       # hud 模式需要 BetterHud
    popup: itemcore_mana_warning
    duration: 30             # tick
    gui_x: 50
    gui_y: 60
    scale: 0.8
    message: "<red>法力不足 <gray>(<aqua>{current}</aqua>/<aqua>{cost}</aqua>)</gray></red>"

# 物品绑定系统
bind:
  enabled: true              # 关闭后所有 bind: true 的物品视为普通物品
  notify_on_bind: true       # 绑定时发送聊天提示
  keep_on_death: true        # 绑定物品死亡不掉落

gui:
  name: "物品库"

# Lore 定时刷新（实验性）：让 /ic reload 后玩家背包内物品的 Lore 自动更新
lore_refresh:
  enabled: false
  interval: 100              # tick
```

### 2.3 attributes.yml 属性参数

```yaml
crit:
  default_crit_damage: 150      # 默认暴击伤害百分比（150 = 1.5 倍）
  cooldown:                     # 暴击冷却，仅对 ATTACK 类型伤害生效
    enabled: false
    mode: FIXED                 # FIXED = 固定冷却 / FULL_CHARGE = 满蓄力才能暴击
    fixed_ticks: 20
    full_charge_threshold: 0.95

adaptive_force:
  attack_conversion: 1.0        # 每点适应之力转化的攻击伤害
  spell_conversion: 1.0         # 每点适应之力转化的法术强度

defense_formulas:               # 可用变量 {damage} {armor}
  physical_resist: '{damage} * (1 - {armor} / ({armor} + 100))'
  spell_resist: '{damage} * (1 - {armor} / ({armor} + 100))'

penetration_order: percent_first  # percent_first / flat_first
```

### 2.4 重载配置

```
/ic reload
```

所有配置文件均支持热重载，无需重启。已生成物品的随机属性、随机品质、耐久、绑定信息保存在物品数据中，重载不会改变。

---

## 3. 命令系统

主命令 `/itemcore`，别名 `/ic`。不带参数时显示帮助。

### 3.1 子命令一览

| 子命令 | 用途 | 权限 |
|--------|------|------|
| `help [页码]` | 显示帮助（每页 5 条） | `itemcore.command.help` |
| `open` / `gui` | 打开物品库 GUI | `itemcore.command.gui` |
| `give <玩家> <分类> <物品> [数量] [-s]` | 给予物品，数量 1-64，`-s` 不通知目标玩家 | `itemcore.command.give` |
| `attribute add <玩家> <属性> <数值> <持续tick> <-s\|-r>` | 添加固定值临时属性 | `itemcore.command.attribute` |
| `attribute percent <玩家> <属性> <数值> <持续tick> <-s\|-r>` | 添加百分比临时属性 | `itemcore.command.attribute` |
| `attribute remove <玩家> <属性>` | 移除该属性的全部临时效果 | `itemcore.command.attribute` |
| `attribute clear <玩家>` | 移除玩家全部临时属性 | `itemcore.command.attribute` |
| `element_mastery <add\|percent\|remove\|clear> ...` | 管理临时元素精通，参数同 `attribute` 但属性换成元素 ID | `itemcore.command.element_attribute` |
| `element_resist <add\|percent\|remove\|clear> ...` | 管理临时元素抗性 | `itemcore.command.element_attribute` |
| `bind <持有者> <新主人\|clear>` | 更换持有者主手物品的绑定对象，`clear` 解除绑定 | `itemcore.command.bind` |
| `reload` | 热重载全部配置 | `itemcore.command.reload` |

**示例：**

```
/ic open
/ic give Is_Lianhua melee_weapons legendary_blade
/ic give Is_Lianhua melee_weapons legendary_blade 1 -s
/ic attribute add Is_Lianhua attack_damage 10 600 -s
/ic attribute percent Is_Lianhua attack_damage -20 200 -r
/ic attribute remove Is_Lianhua attack_damage
/ic element_mastery add Is_Lianhua LIUHUO 10 600 -s
/ic element_resist percent Is_Lianhua HANSHUANG 20 600 -r
/ic bind Is_Lianhua Steve
/ic bind Is_Lianhua clear
/ic reload
```

### 3.2 give 指令说明

- 物品必须同时给出**分类 ID** 与**物品 ID**，因为不同分类允许同名物品（详见 [5.1 物品身份](#51-物品身份)）。
- Tab 补全先列出分类，再只列出该分类内的物品。
- 背包已满时剩余物品掉落在玩家脚下。

### 3.3 临时属性规则

- 属性名使用小写配置键，如 `attack_damage`、`crit_chance`，大写也可识别。
- `-s` 叠加：保留已有同属性效果，每条效果独立计时。
- `-r` 替换：先移除该属性已有的全部固定与百分比效果，再添加新效果。
- 固定效果先相加，百分比效果随后逐层乘算。`+10` 与 `-20%` 的结果为 `(原值 + 10) × 0.8`。
- `percent` 只乘算 ItemCore 属性容器内的值，不包含基础生命 20、基础移速 0.1、无武器基础攻速 4 和基础法力。
- 正数增益、负数减益。`damage_reduction` 为负会增伤，`attack_damage` 可降为负但最终伤害不低于 0。
- 临时属性仅保存在内存，玩家下线后仍按服务器 tick 计时，服务器重启后清除。
- `element_mastery` / `element_resist` 与普通属性分开存储，`clear` 只清除对应类型。

### 3.4 bind 指令说明

- 操作的是持有者**主手**中的物品，且该物品配置必须开启 `bind: true`。
- 新主人可以离线，但必须是服务器曾经记录过的玩家名。
- `clear` 解除绑定后，物品再进入任何玩家背包时会重新绑定给该玩家。

---

## 4. 权限系统

| 权限节点 | 默认 | 说明 |
|----------|------|------|
| `itemcore.admin` | OP | 管理权限（包含以下全部） |
| `itemcore.command.help` | 所有人 | 查看帮助 |
| `itemcore.command.gui` | OP | 打开物品库 GUI |
| `itemcore.command.give` | OP | 给予物品 |
| `itemcore.command.attribute` | OP | 管理临时属性 |
| `itemcore.command.element_attribute` | OP | 管理临时元素属性 |
| `itemcore.command.bind` | OP | 更换 / 解除物品绑定 |
| `itemcore.command.reload` | OP | 重载配置 |
| `itemcore.gui.obtain` | OP | 在 GUI 中点击获取物品 |

`plugin.yml` 中仍保留 `itemcore.command.list`、`itemcore.command.info`、`itemcore.command.repair` 三个旧权限，对应命令已移除或未注册，这些权限当前没有作用。

物品与分类还可配置各自的 `permission` 字段，缺少权限的玩家无法从 GUI 获取，物品的属性、效果、技能、套装件数也不会生效。

---

## 5. 物品创建

在 `items/` 目录下、由分类 `items_file` 指定的文件中创建物品。每个顶级键就是一个物品 ID。

### 5.1 物品身份

物品完整 ID 为 `分类ID:物品ID`，例如 `melee_weapons:legendary_blade`。物品属于哪个分类由 `categories.yml` 中 `items_file` 指向的文件决定，因此不同分类可以使用相同物品 ID。

- 分类 ID 与物品 ID 不区分大小写，不能包含冒号。
- 命令和 API 推荐始终使用 `分类ID + 物品ID` 或完整 ID。
- 新生成的物品会在 PDC 中同时保存完整 ID 与本地 ID；旧版本只有本地 ID 的物品会自动迁移。

### 5.2 完整配置项

配置键统一使用下划线写法（`display_name`、`active_slots`、`item_flags` 等）。

| 配置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `material` | 材质名 | **必填** | 物品材质 |
| `display_name` | 字符串 | — | 显示名称（支持 `&` 颜色码） |
| `display_type` | 字符串 | 所属分类 ID | `#type#` 显示文本，不影响分类规则；旧键 `type` 仅作兼容 |
| `rank` | 品质 ID / 随机模板 ID | — | 引用 `ranks.yml`，详见 [5.5](#55-品质) |
| `lore` | 列表 | — | 物品描述，通过 `#item_lore#` 显示 |
| `color` | RGB | — | 皮革护甲或药水颜色，如 `255, 0, 0` 或 `[255, 0, 0]` |
| `set` | 字符串 | — | 所属套装 ID，见 [9. 套装系统](#9-套装系统) |
| `attributes` | Map | — | 属性，支持固定值或 `min/max` 范围，见 [5.4](#54-属性与随机属性) |
| `random_attributes` | Map | — | 生成时按权重抽取的随机词条 |
| `enchantments` | Map | — | 附魔，如 `sharpness: 3` |
| `item_flags` | 列表 | — | ItemFlag，如 `HIDE_ATTRIBUTES` |
| `enchantment_glint` | 布尔 | false | 只显示附魔光效，不添加真实附魔 |
| `unbreakable` | 布尔 | false | 原版不可破坏 |
| `durability` | 整数 | — | 自定义最大耐久，见 [5.6](#56-自定义耐久) |
| `durability_break` | 布尔 | true | 耐久归零时是否销毁物品 |
| `max_stack` | 整数 | 原版规则 | 最大堆叠数 |
| `custom_model_data` | 整数 | — | 自定义模型数据 |
| `active_slots` | 列表 | `any` | 生效装备位，见 [5.3](#53-active_slots-生效装备位) |
| `effects` | Map | — | 装备时给予的药水效果 |
| `skills` | Map | — | 技能配置（需 ItemCoreMythic），见 [拓展1](#拓展1-技能系统itemcoremythic) |
| `type_settings` | 字符串 / 列表 / Map | — | 分类行为专属配置，见 [5.8](#58-分类与分类行为) |
| `permission` | 字符串 | — | 使用权限；缺少权限时属性、效果、技能、套装均不生效，且无法点击交互或从 GUI 获取 |
| `bind` | 布尔 | false | 物品绑定，别名 `soulbound`，见 [5.9](#59-物品绑定) |
| `keep_on_death` | 布尔 | false | 死亡时保留在背包 |
| `droppable` | 布尔 | true | 是否允许丢弃；为 false 时也不能从护甲栏取下 |
| `clickable` | 布尔 | true | 是否允许在背包中点击移动 |
| `right_clickable` | 布尔 | true | 是否允许右键交互（护甲右键穿戴不受限制） |
| `left_clickable` | 布尔 | true | 是否允许左键交互 |
| `disable_anvil` | 布尔 | 全局默认 | 禁止铁砧操作，旧键 `disable_anvil_repair` |
| `disable_enchant` | 布尔 | 全局默认 | 禁止附魔，旧键 `disable_enchanting` |
| `disable_crafting_table` | 布尔 | 全局默认 | 禁止作为合成材料，旧键 `block_crafting_ingredient` |

**item_flags 常用值：**

```yaml
item_flags:
  - HIDE_ATTRIBUTES      # 隐藏原版属性；武器带此标志时原版武器伤害不计入，伤害完全来自 ATTACK_DAMAGE
  - HIDE_ENCHANTS        # 隐藏附魔
  - HIDE_UNBREAKABLE     # 隐藏不可破坏
  - HIDE_POTION_EFFECTS  # 隐藏药水效果
```

### 5.3 active_slots 生效装备位

```yaml
active_slots:
  - main_hand   # 主手
  - off_hand    # 副手
  - head        # 头部
  - chest       # 胸部
  - legs        # 腿部
  - feet        # 脚部
  - trinkets    # 饰品栏（需 ItemCoreTrinkets）
  - any         # 任意位置（默认）
  - never       # 任何位置都不生效，优先于同列表中的其他槽位
```

`active_slots` 同时控制属性、药水效果、技能触发与套装件数。损坏（耐久为 0）、无使用权限或非绑定主人持有的物品不会生效。

### 5.4 属性与随机属性

`attributes` 中的属性可以写固定值，也可以写 `min/max` 范围。范围会在物品**生成时抽取一次**并保存在物品数据中：

```yaml
attributes:
  ATTACK_DAMAGE:
    min: 5
    max: 10
  ATTACK_SPEED: 1.6
  CRIT_CHANCE: 15        # 百分比属性直接填整数 = 15%
  element_mastery:       # 元素精通
    LIUHUO: 20
  element_resist:        # 元素抗性
    HANSHUANG: 10
```

`random_attributes` 会在生成时从 `entries` 中按权重抽取若干种**不同**属性，与基础属性相加：

```yaml
random_attributes:
  count:
    min: 2          # 也可直接写 count: 3；默认 1
    max: 4
  entries:
    CRIT_CHANCE:
      min: 5
      max: 10
      weight: 100   # 相对权重，默认 100
    CRIT_DAMAGE:
      min: 10
      max: 25
      weight: 80
    HEALTH:
      value: 3      # 固定数值
      weight: 30
```

- 数值精确到一位小数；同一属性不会重复抽取。
- 物品库 GUI 预览会完整显示范围（如 `10-12`），不会预先抽取。
- 随机结果只在物品首次生成时抽取，`/ic reload`、Lore 刷新、耐久变化都不会重新随机。

### 5.5 品质

品质定义在 `ranks.yml`：

```yaml
ranks:
  common:
    level: 1              # 品质等级，供附属比较，必须大于 0
    display: '&7普通'      # #rank# 的显示内容，支持颜色码与图片标签
  excellent:
    level: 2
    display: '&a优秀'

random:
  default_random:         # 随机模板，数字为相对权重
    common: 80
    excellent: 20
```

物品中通过 `rank` 引用固定品质或随机模板：

```yaml
coal_helmet:
  rank: excellent          # 固定品质
random_helmet:
  rank: default_random     # 每次生成按权重抽取一次，结果写入物品数据
```

- 固定品质 ID 与随机模板 ID 不能重名。
- 物品库预览中，随机品质物品的 `#rank#` 会逐行显示各品质及概率。
- 修改品质的 `level` / `display` 会影响所有引用它的已有物品；修改物品的 `rank` 不改变已生成物品。
- `rank` 填写了未定义的文本时仍会显示，但品质等级为 0。
- ItemCore 本身不根据品质限制强化次数等玩法。

### 5.6 自定义耐久

```yaml
legendary_blade:
  material: NETHERITE_SWORD
  durability: 250          # 最大耐久
  durability_break: false  # 耐久归零后不销毁，只是失效
```

- 配置 `durability` 后，物品使用独立于原版的耐久系统，每次命中消耗 1 点。
- 耐久为 0 的物品不再提供属性、技能与套装件数；`durability_break: true`（默认）时直接销毁。
- 耐久 10 以下时随机播放警告音效。
- Lore 中通过 `#durability#` 显示，格式由 `stats.yml` 的 `durability` 控制（`{current}` `{max}` `{bar}`）。
- 附属可通过 API 修复、扣除或直接设置耐久。

### 5.7 物品全局保护

`config.yml` 的 `Item_global_settings` 提供三项默认值（均默认 `true`），物品中可用同名键覆盖：

| 键名 | 说明 |
|------|------|
| `disable_anvil` | 禁止铁砧操作（修复 / 合并 / 改名） |
| `disable_enchant` | 禁止附魔（附魔台、铁砧附魔书或附魔物品合并） |
| `disable_crafting_table` | 禁止作为原版合成材料（工作台 / 2x2 / 自动合成器），防止 PAPER 材质物品被合成书本 |

```yaml
craft_material:
  material: PAPER
  disable_crafting_table: false   # 单独放开
```

### 5.8 分类与分类行为

分类定义在 `categories.yml`：

```yaml
categories:
  melee_weapons:
    name: 近战武器
    icon: DIAMOND_SWORD
    slot: 19                      # 在物品库主菜单的位置
    display_name: "&c近战武器"
    lore:
      - "&7各种近战武器"
    items_file: melee_weapons.yml
    permission: itemcore.category.melee   # 可选

  swords:
    parent: melee_weapons         # 子分类只继承父分类的行为规则
    name: 单手剑
    icon: IRON_SWORD
    slot: 24
    items_file: swords.yml
```

`parent` 只继承父分类注册的行为系统，不继承图标、槽位、Lore 与物品文件。父子都注册系统时按父 → 子执行。

**内置分类行为：**

| 分类 ID | 行为 |
|---------|------|
| `ranged_weapons` | 主手直接近战攻击时，该武器的属性不参与伤害计算（弓只对射弹生效） |
| `consumable` | 技能成功触发后消耗一个；支持 `skills.drink` / `skills.eat`，播放完整原版动画后触发 |
| `tools` | 支持 `type_settings.mining_speed` 与 `type_settings.vein_range` |
| `blocks` | 支持 `type_settings` 中的 `no_place`、`infinite_place` |
| 其他分类 | 无额外行为，可由附属插件通过 API 注册 |

**tools 分类专属配置：**

| 配置项 | 类型 | 默认 | 说明 |
|--------|------|------|------|
| `mining_speed` | 数值 | 材质原版速度 | 挖掘速度，对标原版基础值（木=2 石=4 铁=6 钻石=8 下界合金=9 金=12），仅主手生效 |
| `vein_range` | 整数 | 不连锁 | 连锁挖掘范围，仅连锁同类型方块，单次上限 64 个 |

```yaml
miner_pickaxe:
  material: IRON_PICKAXE
  display_name: '&9矿工之镐'
  type_settings:
    mining_speed: 8    # 铁镐拥有钻石镐速度
    vein_range: 3
  active_slots:
    - main_hand
```

- `mining_speed` 只改变速度，不改变掉落判定；与效率附魔叠乘。
- 连锁挖掘逐方块触发 `BlockBreakEvent`，兼容领地保护，正常掉落并消耗耐久。

**blocks 分类专属配置：**

```yaml
decorative_stone:
  material: CHISELED_STONE_BRICKS
  type_settings:
    - no_place          # 禁止放置
builder_stone:
  material: STONE_BRICKS
  type_settings:
    - infinite_place    # 放置后不消耗
```

`type_settings` 的字符串或列表写法会转换为布尔标记（`no_place: true`），Map 写法保持原值，未知键不会被丢弃，供附属分类系统读取。

### 5.9 物品绑定

```yaml
legendary_blade:
  material: NETHERITE_SWORD
  bind: true
```

开启后物品**首次进入某个玩家背包时**绑定给该玩家，无论来自命令、GUI、拾取、任务奖励或其他插件。绑定信息写入物品 PDC，因此不同主人的同种物品不会堆叠。

| 行为 | 结果 |
|------|------|
| 丢弃（Q 键、拖出背包） | 取消并提示 |
| 死亡 | 保留在背包（`bind.keep_on_death`） |
| 其他玩家或生物拾取 | 取消并提示 |
| 放入箱子、末影箱、潜影盒等容器 | **允许** |
| 非主人拿到 | 属性、套装、技能、分类行为、自定义攻速全部失效，只相当于普通物品；原版交互不受影响 |

- 尚未绑定的物品 Lore 显示 `stats.yml` 的 `bind_pending`，已绑定显示 `bind` 并代入主人名字。
- ItemCore 无法拦截其他插件通过 API 完成的转移（交易、仓库等），这类插件需自行调用 `ItemCoreAPI.isItemBound` 拦截。

### 5.10 完整示例

```yaml
fire_sword:
  material: DIAMOND_SWORD
  display_type: '&c近战武器'
  rank: default_random
  display_name: "&c烈焰之剑"
  lore:
    - "&7燃烧一切的烈焰之剑"
  enchantments:
    sharpness: 3
    fire_aspect: 2
  item_flags:
    - HIDE_ATTRIBUTES
  enchantment_glint: true
  durability: 300
  bind: true
  active_slots:
    - main_hand
  attributes:
    ATTACK_DAMAGE:
      min: 10
      max: 14
    ATTACK_SPEED: 1.6
    CRIT_CHANCE: 15
    CRIT_DAMAGE: 30
    PHYSICAL_PENETRATION: 5
    element_mastery:
      LIUHUO: 20
  random_attributes:
    count: 2
    entries:
      PHYSICAL_DAMAGE:
        min: 5
        max: 15
      HEALTH:
        value: 5
        weight: 50
  skills:
    right_click:
      skill: fireball
      provider: mythicmobs
      mana_cost: 20
```

---

## 6. 属性系统

### 6.1 全部属性列表

配置键不区分大小写，`ATTACK_DAMAGE` 与 `attack_damage` 等价。百分比属性直接填整数（`30` = 30%），也可写 `"30%"`。

**攻击类：**

| 配置键 | 显示名 | 百分比 | 说明 |
|--------|--------|--------|------|
| `ATTACK_DAMAGE` | 攻击伤害 | — | 基础攻击力，直接加到伤害上 |
| `ATTACK_SPEED` | 攻击速度 | — | 主手武器上写最终值（`1.6` = 铁剑）；护甲等其他位置上写加成值（基础 4.0） |
| `ATTACK_RANGE` | 攻击范围 | — | 攻击距离加成 |
| `KNOCKBACK` | 击退 | — | 暴击时额外击退倍率 |
| `CRIT_CHANCE` | 暴击几率 | % | 暴击概率 |
| `CRIT_DAMAGE` | 暴击伤害 | % | 额外暴击倍率（总暴击 = 默认 150% + 此值） |

**法术 / 伤害类型：**

| 配置键 | 显示名 | 百分比 | 说明 |
|--------|--------|--------|------|
| `SPELL_POWER` | 法术强度 | — | 法术伤害基础值，供技能公式 `<ic.spell_power>` 使用 |
| `PHYSICAL_DAMAGE` | 物理加成 | % | 物理伤害百分比加成 |
| `SPELL_DAMAGE` | 法术加成 | % | 法术伤害百分比加成 |
| `PROJECTILE_DAMAGE` | 射弹加成 | % | 射弹伤害百分比加成 |
| `ADAPTIVE_FORCE` | 适应之力 | — | 比较攻击伤害与法术强度，转化为较高者的加成 |

**防御类：**

| 配置键 | 显示名 | 百分比 | 说明 |
|--------|--------|--------|------|
| `ARMOR` | 护甲 | — | 原版护甲值；`combat.disable_vanilla_armor` 开启后失效 |
| `PHYSICAL_RESIST` | 物理抗性 | — | 物理减伤，公式可配置 |
| `SPELL_RESIST` | 法抗 | — | 法术减伤，公式可配置 |
| `DAMAGE_REDUCTION` | 伤害减免 | % | 最终百分比减伤，可为负数（增伤） |

**穿透类：**

| 配置键 | 显示名 | 百分比 | 说明 |
|--------|--------|--------|------|
| `PHYSICAL_PENETRATION` | 物理穿透 | — | 固定值物理穿透 |
| `PHYSICAL_PENETRATION_PERCENT` | 物理穿透百分比 | % | 百分比物理穿透 |
| `SPELL_PENETRATION` | 法术穿透 | — | 固定值法术穿透 |
| `SPELL_PENETRATION_PERCENT` | 法术穿透百分比 | % | 百分比法术穿透 |

**生存类：**

| 配置键 | 显示名 | 百分比 | 说明 |
|--------|--------|--------|------|
| `HEALTH` | 生命值 | — | 额外生命（基础 20） |
| `MANA` | 法力值 | — | 额外最大法力（基础 `mana.base`） |
| `MOVEMENT_SPEED` | 移动速度 | — | 额外移速（基础 0.1） |
| `REGENERATION` | 生命恢复 | — | 每秒恢复生命值 |
| `MANA_REGENERATION` | 法力恢复 | — | 每秒额外恢复法力 |
| `LUCK` | 幸运值 | — | 幸运加成 |

**元素属性：**

| 配置键 | 显示名 | 说明 |
|--------|--------|------|
| `element_mastery.<元素ID>` | 元素精通 | 目前核心伤害计算不直接使用，可通过 API 与技能占位符 `<ic.element_mastery.LIUHUO>` 读取 |
| `element_resist.<元素ID>` | 元素抗性 | 降低对应元素伤害与元素积累量，负数为易伤 |

### 6.2 属性叠加顺序

玩家最终属性按以下顺序计算：

1. 各生效槽位物品属性（含随机属性、附属物品属性提供器）
2. 套装激活属性
3. 附属注册的玩家属性提供器（饰品、称号、等级等）
4. 临时属性固定值
5. 适应之力转换
6. 异常状态的 `ATTRIBUTE_MOD`
7. 临时属性百分比

同名属性自动累加。

### 6.3 适应之力

比较 `ATTACK_DAMAGE` 与 `SPELL_POWER`，攻击伤害大于等于法术强度时转为攻击伤害，否则转为法术强度。转化率在 `attributes.yml` 的 `adaptive_force` 配置。

### 6.4 暴击与暴击冷却

- 总暴击伤害 = `attributes.yml` 的 `default_crit_damage`（默认 150%）+ `CRIT_DAMAGE`。
- 暴击冷却只限制 ATTACK 类型伤害（原版普攻、`icdamage` 中 `attacktype=attack`），SKILL 类型不受限制。
- `FIXED` 模式：两次暴击间隔至少 `fixed_ticks`。
- `FULL_CHARGE` 模式：近战蓄力达到 `full_charge_threshold` 才能暴击；射弹与技能攻击不受蓄力限制。

### 6.5 攻击速度

- 主手自定义武器配置了非 0 的 `ATTACK_SPEED` 时，玩家攻速 = 所有生效物品 `ATTACK_SPEED` 之和，武器上直接写最终值即可（`1.6` = 铁剑，`4.0` = 极快）。
- 主手没有自定义武器攻速时，玩家攻速 = 4.0 + 其他物品的 `ATTACK_SPEED` 加成。
- 近战蓄力不足时，`ATTACK_DAMAGE` 会按蓄力比例（最低 20%）折算。

### 6.6 法力值

- `config.yml` 中 `mana.enabled: true` 开启。
- 最大法力 = `mana.base` + 全部 `MANA` 属性；每秒恢复 = `mana.base_regeneration` + 全部 `MANA_REGENERATION`。
- 当前法力保存在玩家 PDC 中，技能通过 `mana_cost` 扣除，法力不足时不触发。
- 法力不足提示可显示在聊天栏（`messages.yml` 的 `mana.not_enough`）或 BetterHud Popup（`config.yml` 的 `mana.hud.message`，支持 MiniMessage 与 `{cost}` `{current}`）。BetterHud 未安装或未就绪时自动回退到聊天栏。

---

## 7. 元素系统

### 7.1 默认元素

| 元素 ID | 显示名 | 图标 | 颜色 | 积累方式 | 异常效果 |
|---------|--------|------|------|----------|---------|
| `LIUHUO` | 流火 | `🔥` | `&c` | 伤害 × 50% | 灼烧：8 秒，每秒 2% 当前生命的流火伤害 |
| `HANSHUANG` | 寒霜 | `❄` | `&b` | 每次固定 25 | 虚弱：4 秒，物理抗性 / 法抗 -30% |
| `LEIZHE` | 雷蛰 | `⚡` | `&e` | 法术强度 × 0.2 | 易伤：6 秒，受到的技能伤害 +25% |

### 7.2 元素配置 elements.yml

```yaml
elements:
  LIUHUO:
    display: "&c流火"
    icon: '🔥'              # 伤害飘字图标（默认 ✦）
    color: '&c'             # 伤害飘字颜色（默认 &f）
    threshold: 30           # 积累阈值，达到后触发异常（默认 100）
    decay_per_second: 1     # 每秒衰减量（默认 5）
    accumulation:
      mode: DAMAGE_PERCENT  # DAMAGE_PERCENT / FIXED / ATTRIBUTE
      value: 0.5            # 每次伤害 × 50% 计入积累
      allow_sources:        # 允许积累的来源，留空 = 全部
        - ATTACK
        - SKILL
    ailment: LIUHUO_DOT     # 达到阈值时触发的异常 ID
```

**积累模式：**

| 模式 | 积累值 |
|------|--------|
| `DAMAGE_PERCENT` | 元素伤害 × `value` |
| `FIXED` | 每次固定 `value` |
| `ATTRIBUTE` | 攻击者 `attribute` × `multiplier`，`attribute` 支持 `SPELL_POWER` 或 `ATTACK_DAMAGE` |

**积累规则：**

- 只有带元素的伤害才会积累；来源为 `ATTACK`（普攻或 `attacktype=attack`）或 `SKILL`（其他 DamageRequest 伤害，包括异常 DOT）。
- 目标的元素抗性会降低积累量，最多降低 75%。
- 达到阈值后触发异常并清零该元素积累。
- 没有元素伤害输入时每秒按 `decay_per_second` 衰减。

### 7.3 异常配置 ailments.yml

```yaml
ailments:
  LIUHUO_DOT:
    display: "&c流火"
    duration: 160           # tick，160 = 8 秒
    refresh_policy: RESET   # RESET / STACK / IGNORE / REPLACE
    max_stacks: 1           # 仅 STACK 策略使用
    triggers:
      - type: DAMAGE_PERCENT
        value: 0.02         # 2% 当前生命
        interval: 20        # 每 20 tick 触发一次

  HANSHUANG_WEAKEN:
    display: "&b寒霜"
    duration: 80
    refresh_policy: RESET
    triggers:
      - type: ATTRIBUTE_MOD
        attribute: PHYSICAL_RESIST
        value: -0.3         # 属性 × (1 - 0.3)
      - type: ATTRIBUTE_MOD
        attribute: SPELL_RESIST
        value: -0.3

  LEIZHE_BREAK:
    display: "&e雷蛰"
    duration: 120
    refresh_policy: RESET
    triggers:
      - type: RESISTANCE_REDUCTION
        value: -0.25        # 受到的技能伤害 +25%
```

**刷新策略：**

| 策略 | 行为 |
|------|------|
| `RESET` | 已存在时重新计时（默认） |
| `STACK` | 叠加层数，每层独立计时，上限 `max_stacks` |
| `IGNORE` | 已存在时忽略新触发 |
| `REPLACE` | 移除旧的，应用新的 |

**触发器类型：**

| 类型 | 说明 |
|------|------|
| `DAMAGE_PERCENT` | 每 `interval` tick 造成 `value` × 当前生命的伤害；伤害带该元素、按法术类型走 ItemCore 伤害流程，不暴击 |
| `DAMAGE_FIXED` | 每 `interval` tick 造成固定 `value` 点原版伤害（不经过 ItemCore 伤害计算） |
| `ATTRIBUTE_MOD` | 持续期间将目标 `attribute` 乘以 `(1 + value)`，可用于任意属性 |
| `RESISTANCE_REDUCTION` | 持续期间提高目标受到的 DamageRequest 伤害（技能、`icdamage`、DOT），`value` 为负数；原版普攻不受影响 |
| `POTION_EFFECT` | 尚未实现 |

三种默认异常有内置粒子效果（火焰 / 雪花 / 紫色尘埃），自定义异常没有粒子。

### 7.4 自定义元素

```java
ItemCore.getElementRegistry().register(new ElementType("ARCANE", "奥术"));
```

注册后在 `elements.yml` 与 `ailments.yml` 中配置积累与异常规则，并可在物品 `attributes.element_mastery` / `element_resist` 与技能 `element=ARCANE` 中使用。自定义元素应在物品配置加载前注册。

---

## 8. 伤害系统

### 8.1 伤害类型

| 类型 | 标签 | 说明 |
|------|------|------|
| **物理** | `PHYSICAL` | 普攻默认类型，吃 `PHYSICAL_DAMAGE` / 物理抗性 |
| **法术** | `SPELL` | 技能默认类型，吃 `SPELL_DAMAGE` / 法抗 |
| **射弹** | `PROJECTILE` | 弓弩等射弹，吃 `PROJECTILE_DAMAGE` |

### 8.2 原版攻击流程（近战与射弹）

```
基础伤害（武器带 HIDE_ATTRIBUTES 时按 1 计，原版武器伤害不计入）
  → + ATTACK_DAMAGE（近战按蓄力比例折算，最低 20%）
  → × 伤害类型加成（PHYSICAL_DAMAGE / PROJECTILE_DAMAGE / SPELL_DAMAGE）
  → 暴击判定（CRIT_CHANCE，受暴击冷却限制）× 总暴击伤害
  → 物理抗性减免（扣除固定 / 百分比穿透后套用 defense_formulas.physical_resist）
  → × (1 - DAMAGE_REDUCTION%)
  → ItemCoreDamageEvent（附属可修改或取消）
  → 扣除主手自定义耐久 1 点
```

- 主手 `ranged_weapons` 物品在直接近战时不贡献属性。
- 非绑定主人或无权限持有的自定义武器按普通物品处理。
- 暴击时根据 `KNOCKBACK` 增强击退。

### 8.3 DamageRequest 流程（技能、`icdamage`、异常 DOT）

```
基础伤害
  → attacktype=attack：+ ATTACK_DAMAGE，× 伤害类型加成，使用玩家暴击属性
    attacktype=skill：伤害由技能公式自行决定，不额外加属性，暴击率按请求（icdamage 为 0）
  → 暴击判定
  → 穿透 = 请求穿透 + 攻击者对应类型的固定 / 百分比穿透
  → 无元素：按类型套用物理抗性或法抗减免
    有元素：跳过物理 / 法抗
  → 元素抗性、异常 RESISTANCE_REDUCTION、DAMAGE_REDUCTION 加法叠加
    （减伤上限 100%，增伤无上限）
  → ItemCoreDamageEvent
  → 吸血（仅 attacktype=attack）
  → 应用伤害 → 元素积累 → 暴击击退
```

### 8.4 元素伤害规则

- 带元素的伤害**跳过**物理 / 法术抗性，只受元素抗性、异常易伤与 `DAMAGE_REDUCTION` 影响。
- 元素抗性为负数时变为易伤。
- 元素伤害造成后才计入元素积累。

---

## 9. 套装系统

物品通过 `set` 加入套装，套装定义在 `sets.yml`：

```yaml
coal_set:
  display_name: "&8煤炭套装"
  activation_mode: cumulative   # cumulative 累计激活 / highest_only 只激活最高档
  lore:                         # 显示在所有套装物品上，位置由 lore.yml 的 #set_lore# 决定
    - '&7套装效果:'
    - '&8[2] 护甲 +5，生命 +10'
    - '&8[4] 物理加成 +15%'
  bonuses:
    2:
      attributes:
        ARMOR: 5
        HEALTH: 10
    4:
      attributes:
        PHYSICAL_DAMAGE: 15
      skills:
        attack:
          provider: mythicmobs
          skill: CoalFlame
          chance: 25
          mana_cost: 10
        damage:
          provider: mythicmobs
          skill: CoalRetaliation
          chance: 20
        block_break:
          provider: mythicmobs
          skill: CoalMiningReward
          chance: 10
          natural_only: true
          blocks:
            - COAL_ORE
            - DEEPSLATE_COAL_ORE
        timer:
          provider: mythicmobs
          skill: CoalAura
          duration: 20
```

**件数规则：**

- 只统计实际生效的 `active_slots`，每个槽位最多计 1 件。
- 两件 `any` 的物品分别放在主手和副手计 2 件；两件只允许 `main_hand` 的物品分别放主副手只计 1 件。
- 损坏、无权限、非绑定主人或 `active_slots: [never]` 的物品不参与计数。
- 套装属性为固定值，不支持 `min/max`。
- 套装技能支持 `left_click`、`right_click`、`attack`、`damage`、`block_break`、`timer`，参数与物品技能相同；达到多个累计档位时各档位分别判定概率。

---

## 10. Lore 展示系统

### 10.1 布局 tooltip/lore.yml

```yaml
general:
  enabled: true

lore_format:
  - '#item_lore#'
  - '#type#'
  - '#rank#'
  - '#bind#'
  - '{bar}'
  - '#attack_damage#'
  - '#attack_speed#'
  - '#health#'
  - '#mana#'
  - '#crit_chance#'
  - '#element_mastery#'
  - '#element_resist#'
  - '{bar}'
  - '#set_lore#'
  - '#durability#'
  - '#ext:itemcoreenhance:sockets#'
```

| 占位符 | 说明 |
|--------|------|
| `#属性名#` | 显示属性值，为 0 时自动隐藏；`#element_mastery#` / `#element_resist#` 逐元素显示 |
| `#item_lore#` | 物品配置中的描述 |
| `#type#` | `display_type`，格式由 `stats.yml` 的 `item_type` 控制 |
| `#rank#` | 品质显示；随机品质在物品库预览中显示各品质概率 |
| `#bind#` | 绑定状态，格式由 `stats.yml` 的 `bind` / `bind_pending` 控制；未开启绑定的物品隐藏 |
| `#set_lore#` | 所属套装的 `lore` |
| `#durability#` | 自定义耐久 |
| `#ext:命名空间:键#` | 附属插件注册的 Lore 区块，必须单独占一行 |
| `{bar}` | 条件区块边界；到下一个分隔符之间没有内容时整段隐藏 |
| `{bar}文本` | 条件区块标题，标题本身不计入内容判断 |
| `{sbar}` | 无条件区块边界，区块为空也显示 |
| 普通文本 | 始终显示，支持颜色码 |

### 10.2 显示格式 tooltip/stats.yml

```yaml
item_type: '&f{value}'
rank: '&6● &7品质: &r{value}'
durability: '&f耐久: &a{current}&7/&a{max}'
bind: '&c已绑定: &7{owner}'
bind_pending: '&c获得后绑定'

attack_damage: '&f攻击伤害: &6<plus>{value}'
crit_chance: '&f暴击几率: &6<plus>{value}%'
element_mastery: '&f{display}精通: &6<plus>{value}'
element_resist: '&f{display}抗性: &6<plus>{value}'
```

- `{value}` 为数值，`<plus>` 自动补 `+` / `-` 号，元素属性可用 `{display}` 代入元素显示名。
- 属性为范围时 GUI 预览显示 `min-max`。
- 将 `item_type` 或 `rank` 设为空字符串可隐藏对应行。

### 10.3 自动刷新

`config.yml` 的 `lore_refresh` 开启后，会按 `interval` 定期扫描在线玩家背包并刷新 Lore，使 `/ic reload` 后的布局修改自动同步到已有物品。玩家较多时请适当加大间隔。

---

## 11. 物品库 GUI

```
/ic open
/ic gui
```

- 主菜单按 `categories.yml` 中的 `slot` 摆放分类图标，点击进入该分类物品列表。
- 列表默认从左到右、从上到下排列；`armor` 分类按列纵向排列。
- **左键**：获取一件物品（需要 `itemcore.gui.obtain` 与物品自身的 `permission`）。
- **Shift + 左键**：查看物品详情。
- 预览中随机属性显示范围、随机品质显示各品质概率，不会预先抽取。

---

## 拓展1 技能系统(ItemCoreMythic)

ItemCoreMythic 1.0.4，依赖 ItemCore 与 MythicMobs。

### 1.1 技能配置

```yaml
skills:
  right_click:
    skill: fireball
    provider: mythicmobs     # 默认 mythicmobs，可省略
    chance: 25               # 触发概率 0-100，默认 100
    mana_cost: 20            # 法力消耗，默认 0
  left_click:
    skill: slash
  attack:
    skill: bleed
  damage:
    skill: counter
  drink:
    skill: healing_drink
  block_break:
    skill: coal_reward
    natural_only: true       # 默认 true，只对非玩家放置的方块触发
    blocks:
      - COAL_ORE
      - DEEPSLATE_COAL_ORE
  timer:
    skill: regen
    duration: 20             # 间隔 tick，默认 20
```

触发键不区分大小写，`Right_Click`、`right_click`、`on_right_click` 均可识别。

### 1.2 触发类型

| 触发类型 | 说明 |
|---------|------|
| `right_click` | 右键触发 |
| `left_click` | 左键触发 |
| `attack` | 主手物品攻击实体时触发 |
| `damage` | 玩家受到最终伤害大于 0 的伤害后触发；伤害来源实体作为技能目标 |
| `block_break` | 主手物品成功破坏方块时触发，施法位置为方块中心 |
| `drink` | `consumable` 分类，完整播放饮用动画后触发，不留下空瓶 |
| `eat` | `consumable` 分类，完整播放进食动画后触发 |
| `timer` | 定时触发，`duration` 为间隔 tick |

- `drink` 与 `eat` 同一物品只能选一种，同时配置时优先 `drink`；动画被打断不触发也不消耗。
- `block_break` 的 `natural_only: true` 时必须配置至少一个有效 `blocks`，否则技能不加载；玩家放置记录保存在区块 PDC 中，重启后仍有效。
- 技能只在物品处于 `active_slots` 允许的槽位、玩家有权限、物品未损坏且为绑定主人时触发。
- `mana_cost` 在槽位、权限、概率等检查全部通过后才扣除；触发事件被取消时退还法力；`timer` 法力不足只跳过当前一次。

### 1.3 icdamage 机制

在 MythicMobs 技能中使用 ItemCore 伤害计算：

```yaml
Skills:
- icdamage{amount="<ic.attack_damage> * 2";type=physical;element=LIUHUO;penetration=5} @EIR{r=5}
- icdamage{amount="<caster.damage>*1.2";type=physical;attacktype=attack;lifesteal=0.1} @target
```

| 参数 | 别名 | 默认值 | 说明 |
|------|------|--------|------|
| `amount` | `a` | `1` | 伤害公式，支持 MM 占位符、`<ic.xxx>`、四则运算与 `%` 写法 |
| `type` | `t` | `spell` | `physical` / `spell` / `projectile` |
| `element` | `e` | `none` | 元素 ID；`fire` / `ice` / `thunder` 分别是流火 / 寒霜 / 雷蛰的别名 |
| `attacktype` | `at` | `skill` | `skill` / `attack`，决定走哪套伤害规则（见 [8.3](#83-damagerequest-流程技能icdamage异常-dot)） |
| `crit` | `c` | `true` | 是否允许暴击；只有 `attacktype=attack` 时才会使用玩家暴击属性，`skill` 类型不会暴击 |
| `penetration` | `p` | `0` | 额外穿透，与攻击者自身穿透相加，支持占位符与公式 |
| `lifesteal` | `ls` | `0` | 吸血比例（0.1 = 10%），仅 `attacktype=attack` 生效 |

参数使用 MythicMobs 标准分号 `;` 分隔。

**`<ic.xxx>` 占位符：**

| 占位符 | 说明 |
|--------|------|
| `<ic.attack_damage>` 等任意属性键 | 施法者的 ItemCore 属性 |
| `<ic.crit_damage>` | 玩家为总暴击伤害（含默认 150） |
| `<ic.health>` | 玩家为最大生命值 |
| `<ic.movement_speed>` | 玩家为总移速 |
| `<ic.element_mastery.LIUHUO>` | 指定元素精通 |
| `<ic.element_resist.LIUHUO>` | 指定元素抗性 |

MythicMobs 怪物作为施法者时，属性来自附属注册的实体属性提供器。

**数学运算示例：**

```
<ic.spell_power> * 0.5        # 50% 法术强度
<ic.attack_damage> * 2 + 10   # 攻击力 × 2 + 10
<ic.attack_damage> * 50%      # 50% 攻击力
```

### 1.4 icheal 机制

```yaml
Skills:
- icheal{amount="<ic.spell_power> * 0.3"} @self
```

`amount` 支持 MM 占位符与单次四则运算，经 ItemCore 治疗流程（`HealingRequest`）处理。

### 1.5 ItemCoreMythic 配置

```yaml
debug: false
providers:
  mythicmobs:
    enabled: true
cooldown-notification:       # MythicMobs 技能冷却提示
  enabled: true
  type: actionbar            # actionbar / chat
  message: "&c技能 &e{skill} &c正在冷却，还需 &e{remaining}&c 秒"
  decimal-places: 1
  throttle-millis: 250
```

---

## 附录1 PlaceholderAPI 占位符

标识符 `itemcore`。所有属性占位符均可使用任意属性配置键，百分比属性返回带 `%` 的数值。

**属性类：**

| 占位符 | 说明 |
|--------|------|
| `%itemcore_attack_damage%` | 攻击伤害 |
| `%itemcore_attack_speed%` | 攻击速度属性值 |
| `%itemcore_attack_range%` | 攻击范围 |
| `%itemcore_health%` | 当前生命值 |
| `%itemcore_max_health%` | 最大生命值 |
| `%itemcore_mana%` | 当前法力值 |
| `%itemcore_max_mana%` | 最大法力值 |
| `%itemcore_mana_percent%` | 法力百分比（0-100） |
| `%itemcore_mana_regeneration%` | 每秒法力恢复 |
| `%itemcore_movement_speed%` | 总移速 |
| `%itemcore_regeneration%` | 生命恢复 |
| `%itemcore_knockback%` | 击退 |
| `%itemcore_luck%` | 幸运值 |
| `%itemcore_spell_damage%` | 法术加成 |
| `%itemcore_physical_damage%` | 物理加成 |
| `%itemcore_projectile_damage%` | 射弹加成 |
| `%itemcore_spell_power%` | 法术强度 |
| `%itemcore_adaptive_force%` | 适应之力 |
| `%itemcore_crit_chance%` | 暴击几率 |
| `%itemcore_crit_damage%` | 总暴击伤害 |
| `%itemcore_armor%` | 护甲 |
| `%itemcore_physical_resist%` | 物理抗性 |
| `%itemcore_spell_resist%` | 法术抗性 |
| `%itemcore_physical_penetration%` | 物理穿透 |
| `%itemcore_physical_penetration_percent%` | 百分比物理穿透 |
| `%itemcore_spell_penetration%` | 法术穿透 |
| `%itemcore_spell_penetration_percent%` | 百分比法术穿透 |
| `%itemcore_damage_reduction%` | 伤害减免 |

**装备类（`<槽位>` 可为 `main_hand`、`off_hand`、`head`、`chest`、`legs`、`feet`）：**

| 占位符 | 说明 |
|--------|------|
| `%itemcore_durability_<槽位>%` | 剩余耐久（自定义耐久优先，否则原版耐久） |
| `%itemcore_max_durability_<槽位>%` | 最大耐久 |
| `%itemcore_durability_percent_<槽位>%` | 耐久百分比 |
| `%itemcore_has_durability_<槽位>%` | 是否有可用耐久（true / false） |
| `%itemcore_has_<槽位>%` | 该槽位是否有物品 |
| `%itemcore_icon_<槽位>%` | 物品的 BetterHud 图标名（`item_材质名`） |

---

## 附录2 开发者 API

完整接口列表见项目 `docs/addon-api.md` 与 `docs/api.md`。附属插件使用 `provided` 依赖 `com.minemart:itemcore:1.0.8`，并在 `plugin.yml` 中声明 `depend: [ItemCore]`。

### 1. 常用入口

```java
import com.minemart.itemcore.api.ItemCoreAPI;

// 物品（推荐始终提供分类）
CustomItem item = ItemCoreAPI.getCustomItem("melee_weapons", "legendary_blade");
ItemStack stack = ItemCoreAPI.getItemStack("melee_weapons:legendary_blade");
ItemCoreAPI.giveItem(player, "melee_weapons", "legendary_blade", 1);

// 玩家最终属性
AttributeContainer attrs = ItemCoreAPI.getPlayerAttributes(player);
double attackDamage = attrs.getAttribute(CustomAttribute.ATTACK_DAMAGE);
double mastery = attrs.getElementMastery(ElementType.LIUHUO);

// 法力
if (ItemCoreAPI.consumeMana(player, 20)) { /* ... */ }

// 自定义伤害
DamageRequest request = DamageRequest.builder()
    .attacker(attacker)
    .victim(victim)
    .baseDamage(100)
    .damageType(DamageTag.SPELL)
    .element(ElementType.LIUHUO)
    .canCrit(true)
    .attackType(AttackType.SKILL)
    .build();
ItemCoreAPI.processDamage(request);

// 治疗
ItemCoreAPI.processHeal(HealingRequest.builder().healer(caster).target(target).amount(20).build());

// 绑定
boolean bound = ItemCoreAPI.isItemBound(stack);

// 耐久
ItemCoreAPI.repairItem(stack, 50);
```

### 2. 可注册的扩展

| 扩展 | 接口 | 用途 |
|------|------|------|
| 玩家整体属性 | `AttributeProvider` | 称号、职业、饰品等不属于单件物品的属性 |
| 实体战斗属性 | `EntityAttributeProvider` | 让怪物、NPC 参与 ItemCore 伤害与抗性计算 |
| 单件物品属性 | `ItemAttributeProvider` | 强化、镶嵌等按物品 PDC 修改最终属性 |
| 现有属性 Lore | `AttributeLoreProvider` | 覆盖 `#attack_damage#` 等属性行 |
| 自定义 Lore 区块 | `LoreProvider` | 通过 `#ext:命名空间:键#` 插入区块 |
| 分类行为系统 | `ItemTypeSystem` | 为分类添加交互、攻击、技能规则 |

### 3. 事件

`ItemObtainedEvent`、`ItemBindEvent`、`ItemSkillTriggerEvent`、`ItemCoreDamageEvent`、`ItemCoreDamageAppliedEvent`、`ElementAccumulationEvent`、`AilmentTriggerEvent`、`AilmentExpireEvent`。

### 4. 注册自定义元素

```java
ItemCore.getElementRegistry().register(new ElementType("ARCANE", "奥术"));
```

---

## 常见问题

**Q: 修改配置后需要重启服务器吗？**
A: `/ic reload` 即可热重载。已生成物品的随机属性、品质、耐久、绑定不会被重载改变；Lore 布局修改可开启 `lore_refresh` 自动同步。

**Q: 旧配置里的 `display-name`、`active-slots` 还能用吗？**
A: 配置键已统一为下划线写法（`display_name`、`active_slots`、`item_flags`、`custom_model_data`、`max_stack`、`keep_on_death`），`active_slots` 内的 `main-hand` 会自动转换，但其他键请迁移。元素配置的 `decay-per-second`、`allow-sources`、`refresh-policy` 也需改为下划线。

**Q: `/ic give` 提示找不到物品？**
A: 现在必须写 `/ic give 玩家 分类ID 物品ID`，分类 ID 就是 `categories.yml` 中的键名。

**Q: 属性为什么不生效？**
A: 依次检查：`active_slots` 是否包含当前槽位；物品是否损坏（耐久 0）；玩家是否有物品 `permission`；物品是否绑定给了别人。

**Q: 多个装备的属性如何计算？**
A: 同名属性自动累加。如两件装备都有 `ATTACK_DAMAGE: 10`，最终 +20。

**Q: 暴击伤害显示 200%？**
A: `CRIT_DAMAGE` 是额外加成。总暴击 = `attributes.yml` 的 `default_crit_damage`（默认 150%）+ `CRIT_DAMAGE`。

**Q: 攻击速度怎么配置？**
A: 主手武器直接写最终值，`1.6` = 铁剑速度，`4.0` = 极快。护甲等位置写的是加成值。

**Q: 百分比属性怎么写？**
A: 直接填整数，`CRIT_CHANCE: 30` = 30%，不是 0.3。

**Q: 为什么自定义武器的伤害和原版武器攻击力无关？**
A: 带 `HIDE_ATTRIBUTES` 的自定义武器会忽略原版武器伤害，只使用 `ATTACK_DAMAGE`。去掉该标志则两者相加。

**Q: 技能 `icdamage` 为什么不暴击？**
A: `attacktype=skill`（默认）的伤害暴击率为 0。需要暴击请使用 `attacktype=attack`，此时会使用玩家的暴击属性并受暴击冷却限制。

**Q: 元素伤害是怎么计算的？**
A: 带元素的伤害跳过物理 / 法术抗性，只受元素抗性、异常易伤和伤害减免影响。

**Q: 如何让物品技能触发？**
A: 安装 ItemCoreMythic，在物品配置的 `skills` 节点配置技能，确保 `active_slots` 包含对应槽位，且开启法力系统时法力足够。

**Q: 绑定物品放进箱子被别人拿走了怎么办？**
A: 这是预期行为，别人拿到后物品只相当于普通物品。可用 `/ic bind <持有者> <主人>` 改绑，或 `/ic bind <持有者> clear` 解绑后重新获得。

**Q: 为什么创造模式拿出来的物品攻速不对？**
A: 这是 Minecraft 创造模式物品栏的机制问题，生存模式或指令获取正常。

---
